/*
function fullAddress(info) {
    if (typeof info !== 'object') {
        return ('please valid the object');

    }
    const result = "my name: " + info.name + ". my home town: " + info.address;
    return result;
}

const person = {
    name: 'imran',
    address: 'naogaon'
}

const totalResult = fullAddress(person);
console.log(totalResult);
*/

/*
const fullName = {
    name: 'imran',
    address: 'rajshahi'
}

function createName(info) {
    const result = info.name + '@' + info.address + '.com';
    return result;
}
console.log(createName(fullName));
*/






